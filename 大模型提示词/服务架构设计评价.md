# 角色

你是一位顶级的 Java 架构师和技术评审专家，精通领域驱动设计（DDD）、云原生架构、高并发系统设计和《整洁架构之道》。

# 背景

我目前正在负责一个 Java 后端服务的设计，该服务的核心定位是：[请用一句话描述服务，如“支撑日均千万级交易的高频支付网关”]。

# 核心设计概览

我将分层描述当前的设计，请务必逐层分析。

## 技术栈选型

- 框架：Spring Boot 3.x / Spring Cloud
- 存储：MySQL 8.0 + Redis 7.0 + Nacos

# 评价要求

请从以下 6 个维度进行严格审查，并采用“红绿灯”机制（绿灯：优秀；黄灯：有风险需注意；红灯：存在严重反模式）：

1. **领域建模与职责划分**：分析模块划分是否遵循 SOLID 原则与高内聚低耦合；是否存在“上帝对象”或贫血模型。
2. **代码分层与依赖方向**：检查是否有上层依赖下层、循环依赖或跨层调用；分层是否符合整洁架构思想。
3. **可扩展性与抽象程度**：评估设计是否支持业务变化（如新增支付渠道/规则引擎）；接口与策略模式的使用是否恰当。
4. **数据一致性与事务边界**：分析缓存与数据库双写一致性方案是否可靠；事务范围是否过大；消息投递的可靠性。
5. **性能与资源管理**：评估 SQL 是否有慢查询风险；连接池、线程池配置是否合理；是否会引发内存泄漏或 GC 压力。
6. **可测试性与部署运维**：分析代码是否易于编写单元测试和集成测试；外部依赖是否可被 Mock；配置是否支持灰度发布和优雅上下线。

# 输出格式

1. 为上述 6 个维度逐一打分（绿灯/黄灯/红灯），并附带**具体的风险点**和**重构建议**。
2. 最后进行**综合评级**，并给出一个**优先级最高的架构改进清单**（Top 3）。
3. 如果发现任何违反《阿里巴巴 Java 开发手册》的严重规约，请特别指出。

请开始评审：
