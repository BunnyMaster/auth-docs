---
description: 终态视角：禁止兼容层、演进叙事与遗留注释
alwaysApply: true
---

# 终态视角（Target State Only）

本仓库是**新项目 / 开发阶段**。修改 = 纠正错误并落到终态，**不是**为错误实现做兼容。

## 代码与架构

- **无废弃/过渡逻辑**：除非用户明确要求，禁止「兼容旧版本/旧字段/旧 Cookie/旧 localStorage」的兼容层、别名、迁移函数、双读双写。
- **错了就改**：删除错误实现与相关注释，直接按目标架构写；不要留「过渡期」「以后再删」的分支。
- **单一事实来源**：禁止需要人工双处同步的结构（主题色 SCSS↔TS 等用户明确允许的双写除外）。
- **命名语义清晰**：禁止仅顺序颠倒、语义极度接近的命名（如 `a-b` vs `b-a`）。

## 文档与注释

- **只写当前终态**：描述「现在怎么做 / 为什么这样设计」。禁止变更史、PR 交接、历史快照、个人备忘、临时 Demo 说明。
- **禁止叙事型注释**：不要写「之前是 X，现在改成 Y」「为了兼容上次」「遗留问题」「旧版曾…」。
- **零死代码**：禁止注释掉的旧代码、已废弃字段说明、「以后可能会用」的占位逻辑。
- **面向新人**：规范只依赖规则本身，不依赖分支名、PR 编号、某次对话上下文。
- **正式指南不引用交接稿**：`TODO/` 下的 PR 交接/个人备忘可以存在，但正式文档正文不得以其为依据或写「变更史见…」。

## 禁止出现的表述（示例）

- 「为了兼容…」「与旧版对齐…」「历史遗留…」「迁移自…」
- 「PR1～PR5」「变更史见…」「开发阶段临时…」「以后再删」
- 非业务域的 `legacy` / `deprecated` / `backward compatible` 叙事

## BAD → GOOD

```md
❌ BAD：变更史见 TODO/样式PR1～PR5交接.md（历史快照，以本文 + 代码为准）。
✅ GOOD：本文说明当前样式架构、约定与开发流程。
```

```md
❌ BAD：旧版曾用 Cookie 存 token，现已迁移到 localStorage，读时自动兼容。
✅ GOOD：access token 持久化在 localStorage；键名与读写入口见对应模块。
```

## 例外（仅当用户明确说出）

- 生产已上线、必须兼容存量数据/客户端
- 第三方协议本身的技术事实（如 S3-compatible API）
- 业务实体本身是「历史记录」（如密码历史日志）
