## 后端问题

### 挂载 ip2region 后服务反复 OOM 重启

IPv6 库约 600MB+，代码整文件进堆，1.5G 容器易被杀。Nacos 关掉 `resolve-ipv6-enabled`，或去掉 v6 / 改 mmap；仅挂 v4 一般没问题。

**处理**：

1. Nacos 关闭 `auth.common.ip-tool.resolve-ipv6-enabled`（只保留 IPv4，推荐）
2. 必须用 IPv6：改用文件/mmap 搜索，或提高容器内存；不要整库进堆

### 时区问题
